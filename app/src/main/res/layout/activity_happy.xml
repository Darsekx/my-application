<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".HappyActivity"
    android:background="@drawable/happy_square"
    >

    <!-- LINE 14: marginTop is 12 instead of 68 or sth, cuz I haven't configured the menu above like the home page -->
    <Button
        android:id="@+id/buttonHappy"
        android:layout_width="411dp"
        android:layout_height="@dimen/dpHeight85"
        android:layout_marginTop="12dp"
        android:text="Happy ｡^‿^｡"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</RelativeLayout>